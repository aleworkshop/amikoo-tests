{
  "name": "amikoo-tests",
  "version": "1.0.0",
  "description": "Playwright e2e tests for kahootlite.vercel.app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@muuktest/amikoo-reporter": "latest"
  }
}
